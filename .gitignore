# === SECRETS — NEVER COMMIT ===
.env
*.env
secrets.json

# === Python ===
__pycache__/
*.py[cod]
*.pyo
*.pyd
.Python
*.egg-info/
dist/
build/
*.egg
.pytest_cache/
.coverage
htmlcov/

# === Local data / logs ===
logs/*.log
data/*.html
data/*.json
data/last_report.html

# === OS files ===
.DS_Store
Thumbs.db
desktop.ini

# === IDE ===
.vscode/
.idea/
*.swp
*.swo

# === State files (committed intentionally — needed for dedup) ===
# state/sent_alerts.json        ← DO commit (tracks alert history)
# state/processed_command_uids.json  ← DO commit (tracks email UIDs)
# config/positions.json         ← DO commit (updated by email commands)
